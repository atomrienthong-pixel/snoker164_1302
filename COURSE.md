# คอร์ส C# + Unity ผ่านเกมสนุกเกอร์ (snoker164_1302)

โปรเจกต์เรียนหลัก — เรียนแบบทำไปเรียนไป ทุกบทจบด้วยของที่เล่นได้จริงในเกม

## ✅ สิ่งที่เรียนผ่านมาแล้ว (อ่านจากโค้ด + git log)

| แนวคิด | อยู่ในไฟล์ไหน |
|--------|----------------|
| วงจรชีวิต MonoBehaviour: Awake → Start → Update | `Test.cs` |
| `Time.deltaTime` — เวลาต่อเฟรม, นับ FPS | `Test.cs` |
| ตัวแปร + `[SerializeField]` โชว์ใน Inspector | ทุกไฟล์ |
| `enum` + `switch` — สีลูกสนุกเกอร์ 8 สี | `Ball.cs` |
| method, `GetComponent`, Renderer, MaterialPropertyBlock | `Ball.cs` |
| Singleton (`static instance`) + Property (get/set) | `GameManger.cs` |
| interface + EventSystem — คลิกลูกแล้วได้แต้ม | `Ball.cs` (IPointerClickHandler) |

## 🎯 แผนบทเรียนต่อจากนี้

### บท 1 — ฟิสิกส์: ทำให้ลูกกลิ้งและชนกันจริง
- `Rigidbody`, `Collider`, Physic Material (ความฝืด/ความเด้ง)
- โต๊ะมีขอบ ลูกชนขอบเด้งกลับ
- **ผลลัพธ์:** โยนลูกใส่กันแล้วฟิสิกส์ทำงานเหมือนโต๊ะจริง

### บท 2 — ตีลูกขาว: เล็ง + วัดแรง
- Vector3 คืออะไร (ทิศทาง + ระยะ) — หัวใจของเกม 3D ทุกเกม
- ยิง Ray จากเมาส์หาตำแหน่งเล็ง, `AddForce` ตีลูกขาว
- กดค้างวัดแรง ปล่อยเพื่อตี
- **ผลลัพธ์:** ตีลูกขาวไปโดนลูกอื่นได้ด้วยเมาส์

### บท 3 — หลุม: ลงหลุมแล้วได้แต้ม (แทนระบบคลิก)
- Trigger Collider + `OnTriggerEnter`
- ย้ายโค้ดแต้มจาก `OnPointerClick` ไปเป็น "ตกหลุม"
- ลูกขาวตกหลุม = ฟาวล์ เอากลับมาวาง
- **ผลลัพธ์:** เกมสนุกเกอร์เวอร์ชันแรกที่เล่นได้จริง

### บท 4 — กติกา: เทิร์นและลำดับลูก
- state machine อย่างง่าย (ตาใคร / ต้องแทงลูกอะไร)
- แดงก่อน → ลูกสี → แต้มตามสี (ใช้ `point` ที่มีอยู่แล้ว)
- **ผลลัพธ์:** เล่น 2 คนสลับเทิร์นได้

### บท 5 — UI: คะแนนบนจอ
- Canvas, TextMeshPro, อัปเดตคะแนนจาก GameManger
- แถบวัดแรงตอนตี
- **ผลลัพธ์:** หน้าจอเกมสมบูรณ์

### บท 6 — เก็บงาน + Build
- เสียงลูกกระทบ, เอฟเฟกต์, ปรับกล้อง
- Build เป็น .exe ส่งให้เพื่อนเล่น

---

## โน้ตจากผู้สอน

- คลาส `GameManger` สะกดผิด (ที่ถูกคือ `GameManager`) — ตอนนี้ยังไม่ต้องแก้
  เพราะเปลี่ยนชื่อไฟล์/คลาสใน Unity ต้องระวังลิงก์ในซีนหลุด เดี๋ยวสอนวิธีแก้ที่ถูกในบทหลัง ๆ
- `Test.cs` เป็นไฟล์ทดลอง จบบท 1 แล้วลบได้
